An implementation of Conway's Game of Life for the Linux Terminal.

Builds using the provided makefile. Just run "make".

Run "./life" to run a randomly generated pattern.

Run "./life -f [filename]" to run a pattern loaded from a file. Three examples are provided.